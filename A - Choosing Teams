#include<bits/stdc++.h>
using namespace std;
int main(){
int n,k,i,j,c=0,y[2000];
scanf("%d %d",&n,&k);
for(i=0;i<n;i++){
scanf("%d",&y[i]);
if(k+y[i]<=5)
    c++;
}
if(c%3==0||c>3)
    printf("%d",c/3);
else
    printf("0");
}
