#include<stdio.h>
int main()
{
    int n,i,count=0;
    char s[50];
    scanf("%d",&n);
    scanf("%s",s);

    for(i=0;i<n;i++)
    {
        if(s[i]==s[i+1])
            count++;
    }
   printf("%d",count);

}
