#include<stdio.h>
int main()
{
   int k,r,i;
   scanf("%d %d",&k,&r);

   for(i=1;i<=1000;i++)
   {
       if(((k*i)%10)-r==0)
        break;
        else if((k*i)%10==0)
            break;
   }
   printf("%d",i);
}
