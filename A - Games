#include<stdio.h>
int main()
{
   int n,h[100],a[100],i,j,c=0;
   scanf("%d",&n);

   for(i=0;i<n;i++){
      scanf("%d %d",&h[i],&a[i]);}
   for(i=0;i<n;i++)
   {
      for(j=0;j<n;j++)
      {
          if(h[i]==a[j])
          {
              c++;
          }
      }
   }
   printf("%d",c);
}
