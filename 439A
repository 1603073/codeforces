#include<bits/stdc++.h>
using namespace std;
int main(){
long long int n,d,i,j,k,l,s=0;
cin >> n >> d;
long long t[n+1];
for(i=1;i<=n;i++){
    cin >> t[i];
    s = s+t[i];
}
if(d-(s+(n-1)*10)>=0){
    cout << ((n-1)*10)/5 + (d-(s+(n-1)*10))/5 << "\n" ;
}
else
    cout << -1 << "\n" ;
}
