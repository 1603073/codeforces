#include<stdio.h>
int main()
{
    int n,i,a=0,b=0,c=0,x,y,z;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d %d %d",&x,&y,&z);
        a=x+a;
        b=y+b;
        c=y+c;
    }
    if(a==0 && b==0 && c==0)
        printf("YES");
    else
        printf("NO");
}
