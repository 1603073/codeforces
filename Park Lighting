#include<bits/stdc++.h>
using namespace std;
int main(){
int t,n,m,i,j,k;
cin >> t;
for(i=1;i<=t;i++){
  cin >> n >> m;
  if((n*m)%2==0)
      cout << (n*m)/2 << "\n";
  else
      cout << (n*m)/2+1 << "\n";
}
}
