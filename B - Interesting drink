#include<bits/stdc++.h>
using namespace std;
int main(){
long n,i,j,q,mid,m1,m2,c=0;
cin >> n;
long x[n];
for(i=0;i<n;i++)
    scanf("%ld",&x[i]);
    sort(x,x+n);
scanf("%ld",&q);
long m[q];
for(i=0;i<q;i++){
    scanf("%ld",&m[i]);
cout << upper_bound(x,x+n,m[i])-x << "\n";
}
}
